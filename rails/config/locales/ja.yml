# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t "hello"
#
# In views, this is aliased to just `t`:
#
#     <%= t("hello") %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# The following keys must be escaped otherwise they will not be retrieved by
# the default I18n backend:
#
# true, false, on, off, yes, no
#
# Instead, surround them with single quotes.
#
# en:
#   "true": "foo"
#
# To learn more, please read the Rails Internationalization guide
# available at https://guides.rubyonrails.org/i18n.html.
# https://qiita.com/shimadama/items/7e5c3d75c9a9f51abdd5
ja:
  views:
    pagination:
      first: "&laquo; 最初"
      last: "最後 &raquo;"
      previous: "&lsaquo; 前"
      next: "次 &rsaquo;"
      truncate: "..."
  activemodel:
    attributes:
      assessment_request:
        branch_id: "店舗ID"
        property_city: "市区町村ID"
        property_address: "住所"
        user_lastname: "姓"
        user_firstname: "名"
        user_lastname_kana: "姓 カナ"
        user_firstname_kana: "名 カナ"
        user_tel: "電話番号"
        user_email: "メールアドレス"
        property_constructed_year: 築年数
        property_room_plan: 間取り
        property_building_area: 査定可能エリア
        property_land_area: 査定可能エリア
        property_exclusive_area: 査定可能エリア
        property_type: 物件種別
  activerecord:
    errors:
      models:
        assessment_request:
          attributes:
            user_tel:
              invalid_format: 'は0で始まる10桁または11桁の数字を入力してください'
            user_firstname:
              invalid_format: 'は漢字、ひらがな、カタカナ、ー（半角ハイフンマイナス）、空白のみ入力可能です'
            user_lastname:
              invalid_format: 'は漢字、ひらがな、カタカナ、ー（半角ハイフンマイナス）、空白のみ入力可能です'
            user_firstname_kana:
              invalid_format: 'はカタカナ、ー（半角ハイフンマイナス）、空白のみ入力可能です'
            user_lastname_kana:
              invalid_format: 'はカタカナ、ー（半角ハイフンマイナス）、空白のみ入力可能です'
